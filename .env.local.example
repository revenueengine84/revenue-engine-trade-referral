# GoHighLevel inbound webhook URL for the waitlist signup form.
# In GHL: Automation -> Workflows -> Create Workflow -> add trigger "Inbound Webhook" -> copy the generated URL here.
# Add the same variable in Vercel's Project Settings -> Environment Variables for production.
GHL_INBOUND_WEBHOOK_URL=
